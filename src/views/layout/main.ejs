<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MejorPrecio</title>
</head>
<body>

    <% if (registroMensaje) { %>
    
        <div>
            <%= registroMensaje %>
        </div>
    
        <% } %>

        <% if (loginMensaje) { %>
        
            <div>
                <%= loginMensaje %>
            </div>
        
            <% } %>

    <%- body %>
    
</body>
</html>